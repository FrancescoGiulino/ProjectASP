%For runtime instantiated GameObject, only the prefab mapping is provided. Use that one substituting the gameobject name accordingly.
 %Sensors.
%tileSensor_x(tile,objectIndex(Index),Value).
%tileSensor_y(tile,objectIndex(Index),Value).
%tileSensor_state(tile,objectIndex(Index),Value).
%gridManagerSensor_startX(gridManager,objectIndex(Index),Value).
%gridManagerSensor_startY(gridManager,objectIndex(Index),Value).
%gridManagerSensor_lastX(gridManager,objectIndex(Index),Value).
%gridManagerSensor_lastY(gridManager,objectIndex(Index),Value).
%gridManagerSensor_nFreeCells(gridManager,objectIndex(Index),Value).
%For ASP programs:
% Predicates for Action invokation.
% applyAction(OrderOfExecution,ActionClassName).
% actionArgument(ActionOrder,ArgumentName, ArgumentValue).
